apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dns-cheetahfox.fullname" . }}-test-connection"
  labels:
    {{- include "dns-cheetahfox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dns-cheetahfox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
